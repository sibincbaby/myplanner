# Discovery Digest — 2026-09-17

## Candidates Screened

| Project | Source | WB/FG/N/DU | Score | Viable |
|---------|--------|-----------|-------|--------|
| [hotseat — Terminal Dashboard for Claude Code and Codex Quota](https://github.com/Maxteabag/hotseat) | GitHub Search API (created 2026-09-15) verified — 6★, Go, tracks accounts, quota, and the work a usage limit interrupted | 1/0/0/1 | 2/4 | ❌ |
| [sessport — Move Agent Sessions Between Claude Code, Codex and Gemini CLI](https://github.com/lanternsmith/sessport) | GitHub Search API (created 2026-09-16) verified — 1★, TypeScript, list, search, hand off and resume natively | 1/0/0/0 | 1/4 | ❌ |
| [scrollback — Recall Past AI Conversations Across Every Coding Agent](https://github.com/SamCuipogobongo/scrollback) | GitHub Search API (created 2026-09-16) verified — 1★, TypeScript, reads local session storage for 12+ agent CLIs | 1/0/0/0 | 1/4 | ❌ |
| [Ledgr — Paste a Bank Statement, a Local LLM Extracts the Transactions](https://github.com/vasile-lazar/Ledgr) | GitHub Search API (created 2026-09-16) verified — 0★, TypeScript, structured extraction for budgeting and spending insights | 1/0/0/1 | 2/4 | ❌ |
| [claude-never-again — Turn Each Fix Into a Hook That Blocks the Repeat](https://github.com/malaysherasia-ai/claude-never-again) | GitHub Search API (created 2026-09-13) verified — 1★, Shell, generates a Claude Code hook, or one line in `LESSONS.md` when a script can't catch it | 1/0/0/1 | 2/4 | ❌ |
| [desk-ai-robot — Flutter Desk Companion With On-Device ASR and TTS](https://github.com/tangbuu/desk-ai-robot) | GitHub Search API (created 2026-09-16) verified — 1★, procedural eye animation, Groq ASR + LLM tool-calling, fully on-device TTS/ASR | 1/0/1/0 | 2/4 | ❌ |
| [cc-range — A Pixel-Art Shooting Gallery Above the Claude Code Prompt](https://github.com/germanfndez/cc-range) | GitHub Search API (created 2026-09-15) verified — 1★, drawn entirely with the new function-hooks surface, zero tokens | 1/0/1/0 | 2/4 | ❌ |
| [phone-superpower — Give Claude Code a Phone That Blocks Mid-Call](https://github.com/johnparkerg/phone-superpower) | GitHub Search API (created 2026-09-15) verified — 3★, JavaScript, the voice agent can't agree to anything until you answer | 1/0/0/0 | 1/4 | ❌ |
| [scalpel-mcp — `get_symbol()` Instead of a Whole File](https://github.com/amritessh/scalpel-mcp) | HN → GitHub (created 2026-09-11) verified — 1★, symbol-level retrieval MCP returning a definition span plus usages | 1/0/0/0 | 1/4 | ❌ |
| [agentsweep — Scan and Quarantine Malicious Skills, Plugins and MCP Configs](https://github.com/byte5digital/agentsweep) | GitHub Search API (created 2026-09-16) verified — 0★, covers skills, plugins, MCP configs, hooks and instruction files | 1/0/0/0 | 1/4 | ❌ |
| [llmtop — htop for Local LLM Backends](https://github.com/huppiflupp/llmtop) | GitHub Search API (created 2026-09-16) verified — 0★, Python, llama.cpp/Ollama/Lemonade plus GPU and NPU, never wakes socket-activated backends | 1/0/0/0 | 1/4 | ❌ |
| [capy-tracker — Terminal-First Task Tracker, One JSON File per Project](https://github.com/ikarys/capy-tracker) | GitHub Search API (created 2026-09-15) verified — 0★, Python, human CLI plus an AI-agent-friendly interface | 1/0/0/0 | 1/4 | ❌ |
| [DocOps — Drop a Card, Pick an Agent, Watch It Work in Its Own Worktree](https://github.com/Oak-AN/DocOps) | GitHub Search API (created 2026-09-16) verified — 4★, Python, Claude Code/Codex/OpenCode/Hermes, review-test-merge from one screen | 0/0/0/0 | 0/4 | ❌ |
| [sim-mirror — Mirror and Drive the iOS Simulator From Claude Code](https://github.com/AndrewKochulab/sim-mirror) | GitHub Search API (created 2026-09-15) verified — 2★, Python, live viewer, agent cursor tracking, token-efficient UI snapshots over MCP | 0/0/0/0 | 0/4 | ❌ |
| [skill-atlas — Map How Your Skills, Agents and MCP Servers Connect](https://github.com/SamYuen101234/skill-atlas) | GitHub Search API (created 2026-09-16) verified — 1★, JavaScript, Mac app, also tracks skill and agent versioning | 0/0/0/0 | 0/4 | ❌ |

_WB = Weekend-buildable · FG = Fills-gap · N = Novel · DU = Daily-utility_

## Screening Notes

- **Zero viable — the eighth zero-viable day in the last eleven runs (08-19, 08-29, 08-31, 09-03, 09-04, 09-06, 09-07, 09-17).** Fifteen candidates cleared `interest_score >= 2` and every one died before reaching 3/4. Eleven died to a saturated lane already logged here or confirmed fresh today, four to a first-party Claude Code feature or an already-installed plugin, and the two that survived novelty (`desk-ai-robot`, `cc-range`) are toys with `daily_utility=0`, which is the honest score rather than a grudging one. Per the 08-31 note this is steady state, not a broken pipeline — the sources were healthy all run.

- **A brand-new extensibility surface appeared this week and the first things built on it are games.** `cc-range` (a first-person pixel-art shooting gallery drawn above the prompt), `lucastononro/cc-subway` (Subway Surfers inside the Claude Code terminal) and `sawzhang/hello-mod` (a live token/cost panel with clickable buttons) are all **function hooks** — a drawing surface above the prompt that costs zero tokens because nothing round-trips to the model. `SaharCarmel/claude-who` (presence and token leaderboards) and `bahaospanov/claude-mods` are the same surface. This is the only genuinely *new* lane found today rather than a new entrant in an old one, and it is worth watching: the demos are games because the surface is three days old, not because that is its ceiling. `cc-range` still scores 2/4 — `novel=1` is real, `fills_gap` and `daily_utility` are not.

- **Session portability and session recall arrived four-at-once and were already dead on arrival.** `sessport` (move a session between Claude Code, Codex and Gemini CLI), `scrollback` (recall past conversations across twelve agent CLIs), `skillsynchq/txcript` and `Aplexica/Aplexica` (resume a Claude Code conversation in Codex, and back) all shipped inside the same 48 hours, which is itself the tell. The kill above all of them is **`ctxrs/ctx` at 1121★** — "git blame, but for agent sessions", integrating Claude Code, Cursor, Copilot CLI, Codex, Pi and OpenCode. Behind it: `pixle-codes/scrollback` (2026-08-25, *identical name and pitch*), `alimoradi296/agent-sessions` and `yossefEl/agent-sessions`, `drobotmax/session-inspector` (Claude Code/Codex/Qwen into SQLite), `jmars/unified-history-mcp`. `fills_gap=0` lands independently: `--continue`, `--resume` and `--fork-session` are first-party, and the `csess` skill installed in this environment already lists sessions for a folder and fetches a whole transcript as JSON.

- **Two HN front-page "launches" were months-old repos, caught by reading `created_at` before writing them up.** `ctxrs/ctx` — the "Show HN: ctx – Git blame that returns the original agent transcript" post — was **created 2026-02-23 and carries 1121★**. `ctxwitch/ctxwitch` ("Git tells you what changed; this tells you what it'll do") was created **2026-06-09**. Both read as fresh launches from their titles alone and neither belongs in a 24-48h digest; both are useful as prior art instead. The star count remains the tell — a two-day-old repo does not have four figures. This is the 08-13 rule earning its keep for the third time.

- **`claude-never-again` is the day's sharpest idea and is boxed in on three sides.** Turning a fix into a `PreToolUse` hook that blocks the mistake structurally — rather than a `CLAUDE.md` line the model may ignore — is a genuinely better shape than "write it down and hope". But the hook mechanism it generates is first-party, this session already has the `hindsight-memory` plugin enabled for exactly the learn-across-sessions job, and the lane has three same-pitch entrants: `Droi-D/hindsight` ("your agent learns from its mistakes instead of repeating them — turns each session's corrections into memory"), `kunalmalvi/reflector-ai-agent` (a Codex/Claude Code plugin for repeated mistakes within a session) and `tusharrworkk-lab/Rektifie` (a self-correcting layer that detects repeated mistakes in execution traces). Worth recording that this lane needed the broaden-the-query rule *twice*: `"agent lessons learned hook repeat mistakes"` and `"claude code hooks prevent repeated errors"` both returned `total_count=0`, and only `"agent learns from mistakes rules memory"` surfaced the three competitors. Two consecutive zeroes remain evidence of bad phrasing, never evidence of a gap.

- **Fifth consecutive personal-finance kill (08-30, 09-03, 09-06, 09-07, 09-17), and the numbers have not moved.** `Ledgr`'s exact thesis — paste or upload a bank statement, a local LLM extracts structured transactions — returns **28 repos** on a plain `"natural language expense tracking llm"` query (`ashikmhd-devops/ExpenseTrackerApp` parsing natural language via Ollama, `Nepul1234/spendwise-ai`, `anujkumarnath/ai-expense-tracker`, `jeganmaniraje/FINTRACK-AI`, `MananVyas01/LocalBudgetAI`, `devroopsaha744/agatha`), with `elgnailng/autoexpense` extracting transactions from bank PDFs since April — before you reach `tomfunk/fungible` 108★, the strict-superset kill (terminal UI *and* desktop GUI, Plaid sync, CSV import, AI assistant, *and* an MCP server). The `expense tracker ai` query returned twenty repos created in the last three days alone, every one of them a variation on the same pitch. **The interest area is real, the gap is not — cite this instead of re-searching the lane.**

- **`agentsweep` lost to the largest single prior-art repo ever recorded in this log.** Scanning and quarantining malicious skills, plugins, MCP configs and hooks is a live problem — "Skill Poisoning turning AI agents into malware droppers" was on HN the same day, alongside `bouncer.run` offering the same check for npm packages and MCP servers. But **`NVIDIA/SkillSpector` carries 17,432★** doing precisely this, with `obielin/skillguard` 20★, `Teycir/SkillsGuard` 15★, `V3r7ig0/skillvet` (an explicit install-time gate for Claude Code and Cowork), `north-echo/waingro` and `Firmislabs/firmis-scanner` behind it, plus a separate 27-result MCP-audit lane (`GaboITB/mcp-shield`, `oathe-ai/oathe-mcp`, `MarPek6/Security-audit-AI-Skills-for-Claude`). `daily_utility=0` regardless — this runs at install time, not daily.

- **Three candidates were killed by this machine rather than by the market, and that is a legitimate score.** `sim-mirror` mirrors the **iOS Simulator**, which cannot run on Linux, so `daily_utility=0` and `fills_gap=0` are structural here even though the work looks good (and the lane already holds `edimuj/agent-device-mcp`, `kkikione999/utell-ios` and `CarolaneLFBV/mcp-xcode-simulator`). `skill-atlas` is a **Mac app**, and is pre-killed anyway by `/skill-doctor` (2.1.261, which lists unused loaded skills *with their context cost*) plus the `ccplug` tool installed here; `NorbertZentai/claude-explorer` shipped the identical pitch the same day. `llmtop` collides by name and pitch with `ProgrammerPeasant/llmtop` 13★ and five others including `glueck-it/ollama-top` from 09-14, and there is no local-LLM rig here to monitor daily.

- **`hotseat`, `DocOps` and `capy-tracker` are three separate confirmations of lanes logged weeks ago.** `hotseat` is the usage/cost dashboard lane (6-deep on 09-03; `mpge/altim`, `jskarbecki/vibemaxxing` and `iotexproject/sidegrade` all shipped again this week) and dies to native `/usage`, `/cost` and `/context`. `DocOps` is the agent-workbench GUI lane — the largest cluster in this log (`op7418/CodePilot` 6447★, `zhukunpenglinyutong/desktop-cc-gui` 4164★) — and fails `weekend_buildable` on its own scope before novelty matters: a board UI plus a four-harness driver plus worktree lifecycle plus review-and-merge is a quarter, not a weekend. `capy-tracker` is the agent-friendly todo lane (`JoeyWangTW/todo-cli` 7★, `strzhao/ai-todo` 4★, `bllyanos/todoing`, `nevtum/nimbletask`) and loses to native `TodoWrite` besides.

- **Source health, for the record.** Twelve GitHub REST search queries plus four follow-ups, seventeen parallel HN Algolia single-word `search_by_date` calls plus one no-query `tags=show_hn` sweep (270 hits), the Product Hunt homepage, and twenty-two `gh api` prior-art queries across four broadening rounds. Reddit remains fully unfetchable. Product Hunt's AI leads were all closed-source SaaS with no public repo (`Thread`, `Voiskey`, `Weave Router 2.0`, `Twigg`, `Kilo Code for iOS/Android` — the last one also dying to first-party Remote Control) and were dropped before the table per the 08-31 rule. One search returned `total_count=0` on four separate occasions and a single-word control query (`notes`, 4428 results) confirmed the tooling was healthy each time — the zeroes were phrasing, as they always are.

## Viable Projects

None today. Fifteen candidates screened, none reached 3/4.

# Discovery Digest — 2026-09-07

## Candidates Screened

| Project | Source | WB/FG/N/DU | Score | Viable |
|---------|--------|-----------|-------|--------|
| [d-code — Unofficial Desktop Client for the Claude Code CLI](https://github.com/HutsuliakDmytro/d-code) | GitHub Search API (created 2026-09-06) verified — 5★, TypeScript, session history, live chat, editor with LSP + debugger, git, plan limits | 0/0/0/1 | 1/4 | ❌ |
| [Agent-Tarmac — Mission Control for Every Local Agent Session](https://github.com/aknakshay/Agent-Tarmac) | GitHub Search API (created 2026-09-05) verified — 3★, TypeScript, live terminals, activity status, restore after reboot | 0/0/0/1 | 1/4 | ❌ |
| [overture — macOS Kanban Harness Where Cards Are Agent Sessions](https://github.com/omerburakpolat/overture) | GitHub Search API (created 2026-09-05) verified — 3★, Swift, cards move themselves as the session progresses | 0/0/0/0 | 0/4 | ❌ |
| [agit — Inspect, Replay, Share and Fork Agent Sessions](https://github.com/thegoodengineers/agit) | HN → GitHub (created 2026-09-06) verified — 1★, TypeScript, "git for running agents" | 1/0/0/1 | 2/4 | ❌ |
| [transcripts-mcp — Search Local Cursor/Claude/Codex Transcripts via MCP](https://github.com/Stormix/transcripts-mcp) | GitHub Search API (created 2026-09-05) verified — 8★, TypeScript, one MCP server across three agent CLIs | 1/0/0/1 | 2/4 | ❌ |
| [toolog — Local Audit Trail for Claude Code Tool Calls](https://github.com/zaghaghi/toolog) | HN → GitHub (created 2026-09-04) verified — 1★, Rust, embedded DB, nothing leaves the machine | 1/0/0/0 | 1/4 | ❌ |
| [Routed — Zero-Token Local Router for Agent Skills](https://github.com/bshea-1/Routed) | HN → GitHub (created 2026-09-06) verified — 9★, TypeScript, sub-20ms skill selection across coding environments | 1/0/0/1 | 2/4 | ❌ |
| [flutter-no-slop — Teach Your Agent to Stop Over-Nesting Widgets](https://github.com/theamazingyogita/flutter-no-slop) | GitHub Search API (created 2026-09-06) verified — 1★, agent skill, "not every Container needs a SizedBox" | 1/0/0/1 | 2/4 | ❌ |
| [WeChat AI Finance Assistant — Natural-Language Expense Tracking Skill](https://github.com/Vince01875/WeChat-ai-finance-assistant-skill) | GitHub Search API (created 2026-09-06) verified — 1★, Python, local-first, review-before-commit, budget alerts | 1/0/0/1 | 2/4 | ❌ |
| [Likh Lo — Voice-First AI Khata and Ledger Copilot](https://github.com/akanshasingh30nov-creator/likhlo-ai) | GitHub Search API (created 2026-09-05) verified — 2★, Python, Whisper + FastAPI + SQLite WAL, WhatsApp reminders, UPI links | 0/0/0/0 | 0/4 | ❌ |
| [clift — Paste a Screenshot Into the Agent on Your Server](https://github.com/leazoot/clift) | GitHub Search API (created 2026-09-05) verified — 1★, Rust, end-to-end sealed, relayed as ciphertext, delivered once | 1/0/0/0 | 1/4 | ❌ |
| [nimvarya — Let Coding Agents Drive Chrome Without Stealing Focus](https://github.com/Basiliskin/nimvarya) | HN → GitHub (created 2026-09-05) verified — 1★, TypeScript, no description published yet | 0/0/0/0 | 0/4 | ❌ |
| [ontoplano — FOSS Life Management App With an MCP Server](https://github.com/ontoplano/ontoplano) | HN → GitHub (created 2026-09-05) verified — 4★, TypeScript, "managing life, one week at a time", desktop + mobile + MCP | 0/0/1/1 | 2/4 | ❌ |
| [loopgraph — The Agent Builds Its Own Work/Gate/Correct Loop](https://github.com/kevinchiha/loopgraph) | GitHub Search API (created 2026-09-05) verified — 3★, Python, Temporal + LangGraph, audit by an agent that never saw the code written | 0/0/1/0 | 1/4 | ❌ |

_WB = Weekend-buildable · FG = Fills-gap · N = Novel · DU = Daily-utility_

## Screening Notes

- **Zero viable — the seventh zero-viable day in the last nine runs (08-19, 08-29, 08-31, 09-03, 09-04, 09-06, 09-07).** Ran the 08-18 prior-art rule against all fourteen (search the candidate's own one-line thesis, then broaden the phrasing before crediting `novel=1`). Nine died to an already-logged saturated lane, three to a first-party Claude Code feature or an already-installed plugin, and the two that survived novelty (`ontoplano`, `loopgraph`) failed `weekend_buildable` before novelty mattered. Per the 08-31 note this is steady state, not a broken pipeline.

- **The single biggest cluster today was the agent-workbench GUI, and it came in three separate flavours on the same day.** `d-code` (desktop client), `Agent-Tarmac` (mission control across every session on the machine) and `overture` (Kanban board where cards are sessions) are all the same lane, and that lane is not merely deep — it is *enormous*. `op7418/CodePilot` 6447★, `zhukunpenglinyutong/desktop-cc-gui` 4164★ (Tauri, multi-engine: Claude Code, Codex, Gemini, OpenCode, DeepSeek), `ttnear/Clarc` 365★ (native macOS SwiftUI), `ykdojo/safeclaw` 183★ (each session in its own container, with a dashboard), `sverrirsig/claude-control` 132★, `trillion-labs/claude-code-orchestrator` 21★, `amahpour/claude-code-command-center` 9★, `ralbu85/AgentBoard` 6★, `assada/claude-dash` 4★, `knqiufan/Misaka` 30★. All three score `novel=0` without a second query. `overture` additionally scores `daily_utility=0` here — it is Swift/macOS-only and this box is Linux — and `d-code`'s pitch (an editor with LSP *and* a debugger *and* git *and* live chat) is a quarter's work, not a weekend, so `weekend_buildable=0` lands before anything else.

- **`agit` and `transcripts-mcp` are the same object viewed from two ends, and both ends are already occupied.** For the viewer end: `S40911120/recensa` 71★ (self-hosted web viewer — read, search, replay, audit), `karanb192/hindcast` 10★, `QJ-Chen/agentlen` 5★, `tanghong123/claude-replay` 2★, `ashawareb/pitwall`, `cjohnkim/claude-replay-viewer` — on top of the session-viewer lane already logged 09-04 (`claude-session-viewer` 42★, `codex-history-viewer` 28★). For the MCP-query end: `recursive-vibe/smart-fork` 8★ (semantic search over past sessions, exposed as MCP), `the-cloud-clockwork/agentibridge` 4★ (indexes CLI transcripts, 16 tools), `MohammadYusif/agentslog` (transcripts as a local SQLite database). `agit`'s one genuinely distinct verb is **fork** — and forking a session is the branching-conversation lane, which was scored 8-deep and automatic-`novel=0` on 09-06. Both also fail `fills_gap` against first-party `/resume`, `--continue`, and `--fork-session`.

- **`toolog` is a clean example of a candidate that is well-built and still fills no gap.** Local-first, Rust, embedded DB, no telemetry — and Claude Code already ships `PreToolUse`/`PostToolUse` hooks that see every tool call plus a native OpenTelemetry exporter, so the capture layer is a settings entry, not a project. The lane is 6-deep independently: `killertcell428/aigis` 54★, `aiquilibria/contAIned` 5★, `yyy900/claude-night-guard` 2★, `selimllc/cc-audit` (PreToolUse hook logging every tool call), `mahidhar96/burnout` (hash-chained audit log), `NickCirv/claude-audit-log`, plus `nullarch/attemptdb` and `michaelzph/vantrace` from the observability side. Note this sits adjacent to — but is not identical to — the action-firewall lane killed on 09-06; `toolog` only records, it does not block, and it dies anyway.

- **`Routed` is killed twice over, once by the platform and once by this toolkit.** `/skill-doctor` shipped 2026-09-04 in 2.1.261 and reports unused loaded skills *with their context cost*, and the harness already defers skill listings behind a budget; on top of that, `ccplug` is installed here specifically to enable only the plugins and skills a project needs. The routing lane is also 6-deep and none of it is new: `Sven-Mirana/sublation` 277★ (multi-agent skill governance and shadow routing), `erichare/skillroute` 39★ (index SKILL.md bundles, ranked skill plans), `vishal2612200/agentpack` 25★, `torakagemusha-sudo/torafirma-skill-router` 16★ (single-binary router for large skill libraries without loading them), `appleweiping/WEIPING_WHALE` 134★. Worth recording that the narrow query `"skill selection semantic routing claude"` returned `total_count=0` — the fourth time the broaden-the-query rule has flipped a candidate that read as novel at first pass.

- **`flutter-no-slop` is the day's best interest fit on paper and dies to an already-installed plugin.** The pitch — teach the agent that more widgets is not better code — is real, and Flutter is squarely in the interest profile. But `vgv-ai-flutter-plugin` is already enabled in this environment and ships twelve Flutter skills (`bloc`, `layered-architecture`, `material-theming`, `navigation`, `testing`, `accessibility`, `animations`, `internationalization`, `ui-package`, `static-security`, `license-compliance`, `very-good-analysis-upgrade`), which is the same job done by the people who maintain `very_good_analysis`. `fills_gap=0` on that alone. The public lane is independently 6-deep: `HoangNguyen0403/agent-skills-standard` 562★, `cleydson/flutter-claude-code` 52★, `anasfik/FlutterGuard` 24★, `Melsaeed276/flutter-agent-skills`, `quangDecember/flutter-agent-skills`, `TOMOKI977/serverpod-flutter-expert`. **New rule: check the enabled-plugin list before scoring `fills_gap` on any language- or framework-specific agent skill** — this is the same kill-class as a first-party feature, just sourced from the local config instead of the changelog.

- **Fourth consecutive run where a personal-finance candidate cleared `interest_score >= 2` and then died to lane depth (08-30, 09-03, 09-06, 09-07).** Today it was two at once. The WeChat finance skill's thesis — record expenses in natural language, review before commit, budget alerts — returns 28 repos on a plain `"natural language expense tracking llm"` query, including `Yenefa/ai-expense-bot`, `ashikmhd-devops/ExpenseTrackerApp` (local LLM via Ollama), `Nepul1234/spendwise-ai`, `anujkumarnath/ai-expense-tracker`, `jeganmaniraje/FINTRACK-AI`, `AkshitSsharma/ai-expense-assistant` — before you get to `tomfunk/fungible` 108★, the strict-superset kill logged on 09-06. `likhlo-ai` is the voice variant and the voice-ledger sub-lane is itself 4-deep (`VoxLedger`, `VoxLedger_VoiceAIBased_ExpenseLogger`, `sense-ledger-multimodal-expense-tracker`, `unified-ledger-pipeline`); it also fails `weekend_buildable` outright — Whisper plus WhatsApp Business messaging plus dynamic UPI deep links is three integrations, not one, and its target user is a micro-merchant, not this toolkit. **The interest area is real and the gap is not; this is now a standing result, cite it rather than re-searching the lane.**

- **`clift` is the seventh independent implementation of "paste a local screenshot into a remote agent terminal".** `Hackerbone/cssh` 3★, `wavecentral/elwood` 3★ (clipboard bridge for AI, Mac clipboard into remote agent sessions), `drewstone/clipboard-bridge` 2★ and `drewstone/tmux-config` 1★ (both explicitly for Claude Code and Codex over SSH), `leekyungmoon/sshpic`, `rbnkr/snaphost`, `hxy91819/iterm-remote-image-paste`. Its encryption angle (relay sees only ciphertext, one-time delivery) is a genuine differentiator over the rest of the lane, but `daily_utility=0` here — the agent runs on this machine, there is no remote-terminal hop to bridge.

- **`nimvarya` scored zero and is worth a note on how it got there.** Three prior-art phrasings returned `total_count` of 1, 0 and 0, which under the broaden-the-query rule means the phrasings were bad, not that the space is empty — and the real prior art is so established it does not surface on those terms at all: Playwright MCP, Chrome DevTools MCP, `browser-use`, and Anthropic's own Claude-in-Chrome extension. The repo also has **no description and no README pitch published yet**, so the "without stealing focus" claim is a HN title and nothing more. `weekend_buildable` cannot be scored against a spec that does not exist; scored 0 rather than guessed.

- **The two candidates that survived novelty both failed on scope, which is the 09-03 pattern repeating.** `ontoplano` genuinely has no match — `"life management app mcp server"`, `"personal weekly planner ai agent local"` and `"open source life os planner mcp"` all returned nothing comparable — but it is a desktop app *and* a mobile app *and* an MCP server for weekly life planning, which is a quarter of work, and it competes for the same slot as the repo it would live in. `loopgraph` likewise has no direct match, and is built on Temporal *and* LangGraph with a phone-approval gate; standing up a durable workflow engine to get a work/gate/correct loop is the definition of not-a-weekend, and Claude Code's own plan mode plus subagent review covers the useful 80% locally. **No prior art does not imply viable — check scope independently.**

- **Dropped before the table.** On the platform-kill rules: `zhiqingchen/RunWhale` (7★, "brings the full development loop to your phone") → Remote Control, per 09-04; `MakazhanAlpamys/soup-wall` (2★, local firewall over tool calls, tool results, MCP handshakes and subagents) → permission-hardening wave, per 09-06; `PrinceXDev/context-budget-audit` (1★, "the cold start tax") → `/context` and `/skill-doctor`, per 08-17 and 09-06. On already-logged saturated lanes: `okf-memory/okf-agent-memory` (**318★ at two days old** — the 09-06 star-inflation caution applies) and `IRDcode/claude-code-session-handoff` (3★, hand a long session to a fresh one that reads the parent in full) → session-memory/continuity, per 09-03; `Saakar22/DevLog` (1★, AI-structured debugging journal) → the work-log lane, per 09-04; `alpbahadur/interns-review-plugin` (2★, Show HN, "interns must review your agent's design choices") → `fills_gap=0` against first-party `/code-review`, `/security-review` and the `code-reviewer` subagent already installed here; `IulaiJedi/sdd-skill` (2★, spec-driven pipeline with worktrees) → spec-kit lane; `sitkowsp/super-claude-code` (2★, delegate disjoint tasks to Codex/Copilot/Antigravity/Grok/Ollama) → multi-CLI-delegation lane. Also dropped on the 08-30 freshness rule after `created_at` checks: `rashedInt32/claude-hl` (5★, PTY wrapper adding syntax colour to Claude Code output — Show HN today, **created 08-23**) and `manuelschipper/mdmanager` (1★, manage CLAUDE.md/AGENTS.md across machines — HN today, **created 08-18**, and the CLAUDE.md-linting lane is already saturated; `alexvinola/stemma-cli` posted the same day is the same idea).

- **Product Hunt: five AI launches today, zero candidates.** `Kit by Speakeasy` ("your coding agent runtime — Claude but fast, cheap, concise"), `AI Toolbox 3.0`, `Tadata`, `Agentic Video Understanding in Gemini`, `H3 Max by fal`. The three from yesterday that would have been in scope — `dif.sh`, `Reflexio`, `Hyperprobe` — were already screened and dropped on 09-06 under the no-public-repo rule. `/categories/artificial-intelligence` still 404s; the homepage is the only working entry point.

- **Source recipe unchanged and clean.** 15 sequential `gh api search/repositories` sweeps (`created:>2026-09-04`, `sleep 2` between) → 329 in-window repos after dedup against `state/seen.json`, narrowed by an AI-keyword filter on the description, and every shortlisted repo confirmed by a direct `repos/<owner>/<name>` fetch rather than trusted from the search page. 17 parallel HN Algolia single-word `search_by_date` calls plus one no-query `tags=show_hn` sweep with `created_at_i%3E1788566400` → 100 relevant stories in the 48h window. 29 further `gh api` calls for prior-art verification across three broadening rounds. Claude Code changelog checked for new first-party kills: only 2.1.263 (2026-09-06, bug fixes) since the last run. Reddit remains fully unfetchable and was not attempted.

# Discovery Digest — 2026-08-23

## Candidates Screened

| Project | Source | WB/FG/N/DU | Score | Viable |
|---------|--------|-----------|-------|--------|
| [Leutenegger/claudish-to-english — verbatim clone of a 2082★ repo, 580★ in one day](https://github.com/Leutenegger/claudish-to-english) | GitHub search (`sort=stars`) · Shell, MIT, created 08-21 · README md5-identical to the original | 1/1/1/0 | 3/4 | ✅ |
| [dlhck/strip-clanker-attribution — strip AI co-author trailers in a commit-msg hook](https://github.com/dlhck/strip-clanker-attribution) | GitHub · 2★, JS, MIT, created 08-21 | 1/1/0/1 | 3/4 | ✅ |
| [mcxiaochenn/AiUsage — Flutter+Rust local-first mobile monitor for Codex usage limits](https://github.com/mcxiaochenn/AiUsage) | GitHub · 2★, Dart, MIT, created 08-21 · signed Android v0.1.0 | 1/0/0/1 | 2/4 | ❌ |
| [isCopyman/cli-delegate — spawn grok/cursor/claude/codex as a child, resume the same session](https://github.com/isCopyman/cli-delegate) | GitHub · 8★, JS, MIT, created 08-21 | 1/1/0/0 | 2/4 | ❌ |
| [chakki-the-potato/open-migrate — move rules/skills/hooks between 4 coding CLIs, 12 directions](https://github.com/chakki-the-potato/open-migrate) | GitHub · 3★, Shell, MIT, created 08-21 · plan-then-undo | 1/0/1/0 | 2/4 | ❌ |
| [willdynan/claude-statusline — 5h/7d rate-limit windows with reset countdowns](https://github.com/willdynan/claude-statusline) | GitHub · 0★, created 08-21 · **one of 13 near-identical statusline repos created in 48h** | 1/0/0/1 | 2/4 | ❌ |
| [adnanakil/nobuzz — /debuzz skill pipes Claude's answer through Gemini to strip the voice](https://github.com/adnanakil/nobuzz) | HN 337p · 169★, MIT, created 08-21 | 1/0/0/0 | 1/4 | ❌ |
| [gvzdv/claudish-to-english — rewrite each assistant message into plain language, local ollama](https://github.com/gvzdv/claudish-to-english) | GitHub · 2082★, Shell, MIT, created 08-10 · origin of the cloned repo above | 1/0/0/0 | 1/4 | ❌ |
| [Mr-AK-AI/agent-office — terminal-native delegation, local CLI agents in named tmux rooms](https://github.com/Mr-AK-AI/agent-office) | GitHub · 3★, Shell, MIT, created 08-21 | 1/0/0/0 | 1/4 | ❌ |
| [Parthuss/stash — Instagram saves → transcribed markdown vault + FTS5 + Claude skill/MCP](https://github.com/Parthuss/stash) | GitHub · 0★, Python, MIT, created 08-20 | 0/0/1/0 | 1/4 | ❌ |
| [iamcheyan/omarchy-voxtype-enhance — Omarchy voice-input plugin, offline models, universal paste](https://github.com/iamcheyan/omarchy-voxtype-enhance) | GitHub · 18★, Python, MIT, created 08-22 | 1/0/0/0 | 1/4 | ❌ |
| [dennis2lee/local-dictation — offline KO/EN dictation typing at your cursor, Whisper on CPU](https://github.com/dennis2lee/local-dictation) | GitHub · 0★, Go, MIT, created 08-21 · client is Windows/macOS only | 1/0/0/0 | 1/4 | ❌ |
| [munder-difflin — agent harness running an "office of your clones", 12 CLI agents](https://github.com/chaitanyagiri/munder-difflin) | HN 222p · 3618★, JS · **created 2026-05-31, not new** | 0/0/1/0 | 1/4 | ❌ |
| [yesheng-oss/context-mcp-subgraph — Tree-sitter code graph + budgeted context retrieval MCP](https://github.com/yesheng-oss/context-mcp-subgraph) | GitHub · 8★, JS, no license, created 08-21 | 0/0/0/0 | 0/4 | ❌ |
| [tiammomo/RepoSteward — local-first policy-gated control plane, issues → verified PRs](https://github.com/tiammomo/RepoSteward) | GitHub · 22★, Python, MIT, created 08-21 | 0/0/0/0 | 0/4 | ❌ |
| [SkillWorks — every Claude Code skill scored nightly on whether it loads](https://skillworks.kynth.studio) | HN Show HN · closed-source service · 503,570 listings, 48,190 that don't load | 0/0/0/0 | 0/4 | ❌ |

_WB = Weekend-buildable · FG = Fills-gap · N = Novel · DU = Daily-utility_

## Viable Projects

- [attribution](../projects/2026-08-23-attribution) — a `commit-msg` hook that enforces the user's own no-AI-attribution rule, which has already failed 4 times in this repo
- [provenance](../projects/2026-08-23-provenance) — check whether a repo is the original or a fresh clone, before installing it or writing it into this digest

Both are small — a hook and a shell function. Each project page says so up front rather than inflating the scope to match the page.

## Screening Notes

- **The most valuable find today is a hole in this pipeline.** `Leutenegger/claudish-to-english` came back 580★, created 2026-08-21, a Claude Code plugin — inside the window, on-profile, and it passed the `created_at` check that memory mandates, because the repo genuinely was created that day. Its README is byte-identical (`md5 09cd4600…`) to [gvzdv/claudish-to-english](https://github.com/gvzdv/claudish-to-english), created 11 days earlier with 2082★. The sweep ranks by `sort=stars`, so a same-day copy can outrank nothing but still land near the top of a page the digest is built from. **`created_at` is necessary and not sufficient; add a content check.** That is `2026-08-23-provenance.md`.

- **The account behind it, stating only what is provable.** `Leutenegger`: created 2026-03-05, 6 public repos, **7 followers**, ~4,150 stars across repos created in the last 10 days (`book-to-skill` 1227★, `watermarks-remover` 933★, `vanity-eth` 803★, `coldcard-airgap` 608★, `claudish-to-english` 580★) — and one repo, `headroom`, with 0. One of those repos is a verbatim copy of an older original; the rest are unverified. Two are crypto-wallet utilities, which is the worst category to install from an unverified source. Counter-evidence worth recording: the fork-to-star ratios look ordinary (145 forks on 1227★), so forks are a weak signal and a naive threshold would produce false positives.

- **13 `claude-statusline` repos were created in 48 hours**, all near-identical, all 0★ (willdynan, raphaelc0elh0, blooop, l0ng-ai, elmofromok, nghiatm-twendee, AbdallaM-Ibrahim, k-sampat, GandzyTM, sergiivelykodnyi, Sarun9624, …), in Python, Go, Zig, PowerShell, and single-binary ports. They all render the same new thing: `5h: 68% | 7d: 35% (4d16h)`. Claude Code's session JSON now exposes both rate-limit windows with reset countdowns, and everyone shipped the same status line the same week. A burst like this is a *saturation* signal — the lane closed before it opened. `/statusline` and the `statusline-setup` agent are native anyway.

- **The Claude-usage lane is closed, and this is the third consecutive kill by the 08-18 prior-art rule.** `AiUsage` (Flutter + Rust, local-first Android monitor for Codex limits) was the strongest Flutter-lane candidate in weeks and scored `fills_gap=0` on one search: `Maciek-roboblog/Claude-Code-Usage-Monitor` 8650★, `getagentseal/codeburn` 9603★, `junhoyeo/tokscale` 5126★, `phuryn/claude-usage` 2166★, `Javis603/token-monitor` 1608★ — plus mobile and Flutter clones already shipped (`CodexBar-Mobile` 57★, `BlueHeisenberg/claudeMeter` PWA+Android, `farhans-codes/ai_limit_status`). Combined with the statusline burst, quota display is the single most over-served category in the ecosystem. **Do not plan anything that renders a usage number.**

- **"Make Claude sound human" was a real cluster this week and is already fixed upstream.** `nobuzz` (169★, HN 337p) pipes Claude's last response through the Antigravity/Gemini CLI to strip the voice; `claudish-to-english` (2082★) rewrites every assistant message via local ollama. Both score `fills_gap=0`: Claude Code shipped a native **`Concise` output style** ([code.claude.com/docs/en/output-styles](https://code.claude.com/docs/en/output-styles), on HN the same day), and the user already runs Ponytail-style output rules that do this at the source. Spending a second model on post-hoc translation is the expensive way to fix a prompt. This is another platform-fixed-it kill, same class as `/context`.

- **Agent-delegation had six same-week entries and is saturated.** `agent-office` (tmux rooms), `cli-delegate` (spawn grok/cursor/codex as a child and resume it), `munder-difflin` (3618★ but created 05-31 — an HN repost, not new), `oh-my-subagents`, `codex-subagent-plugin`, `zcode-dsh-bridge`. The one genuinely interesting idea in the cluster is `cli-delegate`'s **cross-subscription arbitrage** — keep Claude as orchestrator, send grunt work to a CLI you're already paying for separately — which the native Agent tool cannot do. It scores 2/4 only because daily utility depends on holding multiple subscriptions. If that ever becomes true, install `cli-delegate`; don't build it.

- **`strip-clanker-attribution` is the rare case where a 2★ repo beats its own category.** The user's global `CLAUDE.md` forbids AI attribution trailers, and `git log --grep='Co-Authored-By' --all` in this repository returns **4 commits that have them** — `a9dfb54`, `938e7a7`, `32aae58`, `cdf6218`, all four from `[cloud]` runs. Cloud agents don't load the user's global `CLAUDE.md`, so the rule silently doesn't apply there. `core.hooksPath` is already set to `~/.git-hooks`, which today contains only a `post-checkout` hook. The gap is measured and the fix drops into infrastructure that already exists.

- **Reddit stayed dead, per 08-19.** `.json` died 08-17, `.rss` returned 403 on 08-19, `old.reddit` needs a login. No calls were spent on it. HN Algolia carried the leg: 14 parallel single-word `search_by_date` queries plus a no-query `tags=show_hn` sweep returned **358 stories** in the 48h window. Product Hunt's homepage worked and contributed Wispr Flow and "Agents Never Sleep" (both below threshold).

- **Source recipe held with no false negatives.** 14 sequential `gh api search/repositories` calls with `sleep 2` (all returned a full 25 items — no rate-limit zeros), then per-repo `gh api repos/<o>/<n>` confirmation. Reading `created_at` demoted seven HN "launches" that were reposts of older repos: `dat999zx/knowl` (06-24), `ClachDev/Voro` (07-08), `henryqin1997/statem` (06-05, 324★), `gtapps/claude-code-hermit` (03-23), `proliferate-ai/proliferate` (04-30), `ringlochid/oh-my-subagents` (07-20), `baturyilmaz/readingisfun` (03-15, last pushed 03-22). `ldbumble/taskuary` was already in `state/seen.json` from 08-19.

- **Finance produced nothing again, and this time the lane itself is the finding.** Every `expense`/`budget`/`finance` repo created in the window is a 1★ student project (`expense-tracker`, `ExpenseX`, `campus-expense-tracker`, …). With 14 unbuilt finance plans already on file, the honest read is that this lane is not under-served by the ecosystem — it's over-served by *this pipeline*. Stop generating finance plans until an existing one gets built.

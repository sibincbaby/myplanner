# Discovery Digest — 2026-09-06

## Candidates Screened

| Project | Source | WB/FG/N/DU | Score | Viable |
|---------|--------|-----------|-------|--------|
| [codenotch — Usage Limits Pinned to a Screen Edge](https://github.com/vinzdg/codenotch) | GitHub Search API (created 2026-09-05) verified — 133★, Swift, macOS, covers Claude Code + Cursor + Codex + Antigravity | 1/0/0/1 | 2/4 | ❌ |
| [claude-adhd — Gentle Reminders for Unfinished Threads](https://github.com/shaheer-00/claude-adhd) | GitHub Search API (created 2026-09-05) verified — 50★, JavaScript, Claude Code plugin, marked "(test mode)" | 1/0/0/1 | 2/4 | ❌ |
| [Stroq — Local Action Firewall for Coding Agents](https://github.com/AGGIB/Stroq) | GitHub Search API (created 2026-09-04) verified — 6★, TypeScript, taints the session on what the agent reads | 1/0/0/0 | 1/4 | ❌ |
| [evolving-mcp — An MCP Server That Writes Its Own Tools](https://github.com/anshulforyou/evolving-mcp) | GitHub Search API (created 2026-09-04) verified — 6★, TypeScript, tools synthesized from usage patterns | 1/0/0/0 | 1/4 | ❌ |
| [PiX — Non-Linear Agent Workbench (Session as a Tree)](https://github.com/huang-sh/PiX) | GitHub Search API (created 2026-09-04) verified — 3★, TypeScript, branch anytime, context follows the branch | 0/0/0/1 | 1/4 | ❌ |
| [redactproxy — Keep Client Data Out of Claude Code](https://github.com/CSPF-Founder/redactproxy) | GitHub Search API (created 2026-09-05) verified — 3★, Go, local redaction proxy | 1/0/0/0 | 1/4 | ❌ |
| [codegraph-explorer-skill — Codebase as a Queryable Knowledge Graph](https://github.com/GoldDHacker/codegraph-explorer-skill) | GitHub Search API (created 2026-09-04) verified — 3★, Python, parsed once locally so the LLM never re-reads source | 0/0/0/1 | 1/4 | ❌ |
| [Attic — Findings That Survive `/compact`](https://github.com/NishikantaRay/Attic) | GitHub Search API (created 2026-09-04) verified — 3★, JavaScript, writes findings to `.attic/` | 1/0/0/1 | 2/4 | ❌ |
| [uiverse-bridge — Point at a UI Element, Agent Gets the Source File](https://github.com/qi001007/uiverse-bridge) | GitHub Search API (created 2026-09-05) verified — 1★, Python, offline Uiverse browser + pick-to-code handoff | 1/0/0/0 | 1/4 | ❌ |
| [Ledger — Local AI-Assisted Budgeting from Bank CSVs](https://github.com/Ppresha/ai-budgeting-app) | GitHub Search API (created 2026-09-05) verified — 0★, JavaScript, consolidates CSV exports, chat with Claude about spending | 1/0/0/1 | 2/4 | ❌ |

_WB = Weekend-buildable · FG = Fills-gap · N = Novel · DU = Daily-utility_

## Screening Notes

- **Zero viable — the sixth zero-viable day in the last eight runs (08-19, 08-29, 08-31, 09-03, 09-04, 09-06).** Ran the 08-18 prior-art rule (search each candidate's one-line thesis, then broaden the phrasing before crediting `novel=1`) against all ten. Every one of them died to prior art, to a first-party Claude Code feature, or to both. Per the 08-31 note, this is the steady-state yield for this task, not a signal that the pipeline is broken.

- **Biggest platform-fixed-it kill today: the permission/sandbox hardening wave of 2.1.247→2.1.261 has closed the "guardrail for coding agents" lane outright.** Per `code.claude.com/docs/en/changelog`, in the last three weeks Claude Code shipped `--restricted` mode (2.1.248 — strips command/code-running tools and `WebFetch`, pins file tools to the working directory, refuses `bypassPermissions`), the auto-mode **Containment Escape** rule (2.1.257 — cloud metadata-credential fetches, egress evasion and cross-tenant reach are no longer auto-approved), a one-time prompt before the first read outside working directories plus `permissions.blockReadsOutsideWorkingDirectories` (2.1.257), `--permission-prompts none` for unattended hosts (2.1.259), and a long run of deny-rule fixes covering Bash option values, `git diff`/`git grep` operands, `cd DIR && cat FILE` compounds, `< file` redirects, reader commands like `tac`/`egrep`, and symlinked search paths (2.1.251/2.1.259/2.1.260). On top of `PreToolUse` hooks, that is the whole job `Stroq` is built for → `fills_gap=0`. **Treat any "block dangerous agent actions / taint the session / command firewall" pitch as dead on arrival from now on**, same kill-class as the 09-04 Remote Control kill.

- **Second platform kill, narrower but clean: `/skill-doctor` shipped in 2.1.261 on 2026-09-04** — it lists unused loaded skills *and their context cost*. That is first-party skill-usage telemetry, and it closes the last gap in the lane the 08-19 skill-telemetry and 09-04 `context-tax` kills were working on. Combined with the first-party `skill-creator`, it is also why **`lattebbrook/agent-skiller`** (30★, visual builder for agent skills, created 09-05) was dropped before the table rather than scored — it is a GUI over a job the platform now ships end to end, and the visual-skill-builder lane is independently 5-deep (`hubertlim/SkillForge` 5★, `mryesiller/claude-code-builder` 4★, `ZSeven-W/agent-skill-visualizer` 3★, `Guangpop/AgentFlowBuilder`, `Bevel-Software/Hexis`).

- **New automatic-`novel=0` lane, and it is the deepest cluster recorded to date: local PII/secret redaction proxies for coding agents — 11 distinct repos, none of them new.** `sgasser/pasteguard` 741★, `occludra/gateway` 37★, `crp4222/PrivAiTe` 37★, `giancarloerra/JanuScope` 29★, `EdamAme-x/pentect` 26★, `outgate-ai/og-local` 11★, `0M3REXE/eidolon` 9★, `sphragis-oss/sphragis` 6★, `Lexus2016/LocalGuard` 5★, `aezizhu/scrimward`, `DenEwout/cc-hush`, `CupOfGeo/llm-redact-proxy`. `redactproxy`'s pitch ("a local proxy that keeps client data out of Claude Code") is verbatim the category description. `novel=0`, and with no client-data boundary in a personal-projects toolkit, `daily_utility=0` too.

- **The broaden-the-query rule was decisive again, and this time it flipped the single candidate that would otherwise have scored `novel=1`.** `evolving-mcp`'s narrow phrasing `"self improving mcp tools dynamic"` returned `total_count=0`, and `"mcp dynamic tool registration learn usage"` also returned `total_count=0` — read literally, that is a clean novelty claim. Broadening to `"meta tooling agent writes tools"` and `"agent creates new tools runtime"` surfaced the real cluster: `andreasronge/ptc_runner` 21★, `sdeture/metaprompt-harness`, `Klukai-416-Clukay/dsh-skill-curator`, `Codinghaze-AI/llm-limbo`, `vek199/Meta-tooling-in-Strands-Agents`, `mihaws/swarm_thing_strands`, `reallyreallyryan/cairn` — seven. **Two consecutive zero-hit queries are evidence that the phrasing is too specific, not evidence of a gap.** It also fails `daily_utility` on its own mechanics: a tool surface that mutates as you use it invalidates the prompt cache on every change, which the 2.1.260 changelog now calls out explicitly as a named cause of cache misses.

- **`PiX` is this week's instance of the branching-conversation lane, which is 8-deep and over a year old**: `yxp934/Prompt-Tree` 87★, `Anionex/treeAI` 48★, `PaoloJN/ai-chat-tree` 11★, `pangolinsec/baobab` 8★, `RealPhonyx/Astral-Web-UI` 5★, `Om22210564/AI-Memory-Tree` 5★, `bhc6/ChatTree` 4★, `itzabhishekgour/TinexusFlow`. It also fails `weekend_buildable=0` before novelty even matters — a tree UI plus per-branch context routing plus a workbench shell is not a 1-2 session MVP, the same scope failure logged for `matou` on 09-04.

- **`claude-adhd` and `Attic` are both the 09-03 session-memory/continuity lane, which is already on the automatic-`novel=0` list** (`motif`, `jarvis-os`, `elepha`, `agentslate`, `sdlc-kit`). A fresh check added `CharlesXiangqianLIU/vibe-tracker` (captures mid-task ideas, merges them into the todo list, keeps them across sessions — `claude-adhd`'s pitch exactly), `tomohiro-owada/cc-plan-viewer`, `cristianj26/mem`, and `deepmemteam/deepmem-claude-plugin` (Show HN today). Both also fail `fills_gap` against first-party `TodoWrite` persistence, `/resume`, `--continue`, and the 2.1.260 auto-compact improvements. **Worth flagging that `claude-adhd` carries 50★ at one day old while its own description still says "(test mode)"** — plausible early-star inflation, and a reminder that star count inside the 48h window is not a quality signal.

- **`codenotch` is the usage/cost-dashboard lane (automatic `novel=0` since 09-03: `clawdeck`, `trayt`, `devie-ai-quota-tracker`, `burn`, `LLMSwapper`, `claude-kicker`) plus a first-party kill.** 2.1.251 added the Spend limit bar to `/usage` and the `rate_limits.spend_limit` status-line field, and the per-session prompt-cache line to `/cost`; 2.1.260 added the likely *cause* of each cache miss. Its multi-vendor angle (Cursor/Codex/Antigravity alongside Claude Code) is the only part the platform does not cover, and that is not a gap in this toolkit. Highest star count of the day at 133★ and still a 2/4.

- **`codegraph-explorer-skill` is 9-deep** (`brandondocusen/CntxtPY` 115★, `senoldogann/LLM-Context-Manager` 61★, `bcallender/agent-context-workshop` 16★, `cognitx-leyton/codegraph` 11★, `o-samoilov/quasar-graph`, `vanshksingh/DeepFlow`, `jeremiepas/Graphos`, `kogriv/codemap`, `TatievskiArik/MCP-Code-Analyzer`) and fails scope the same way `PiX` does — a correct multi-language code graph is not a weekend. **`uiverse-bridge` loses to prior art that predates the AI angle entirely**: `ericclemmons/click-to-component` 2350★ (2022), `aaamoon/react1s` 223★, `ArnaudBarre/vite-plugin-react-click-to-component` 84★, plus `evankoza/element-source-pointer` for the coding-agent variant specifically.

- **`Ledger` is the day's best interest fit — personal finance, local-first, Claude in the loop — and it still loses on every axis but effort.** `tomfunk/fungible` (108★) is a strict superset: terminal UI *and* desktop GUI, Plaid sync, CSV import, an AI assistant, and an MCP server. Behind it: `akutishevsky/lunchmoney-mcp` 101★, `ignaciohermosillacornejo/copilot-money-mcp` 78★, `fabianonetto/mcp-server-firefly-iii` 23★, plus a deep OSS layer on the app side (`RavindranShanmugam/finance-agent-clean`, `naveenkumar-chandrasekar/FinSight`, `TasniaNitu/finance-ai-assistant`, `FlorianBx/obol`, `mohdrumman1/finance-tracker`, `iParsian/MyBudgetTracker`). This is the third consecutive run (08-30, 09-03, 09-06) where a personal-finance candidate cleared `interest_score >= 2` and then died to lane depth — the interest is real, the gap is not.

- **Product Hunt produced four plausible leads and zero candidates, all dropped by the 08-31 no-public-repo rule**: `dif.sh` ("markdown feature flags your coding agent installs for you"), `Reflexio` ("behavioral learning that makes AI agents better over time"), `Hyperprobe` ("lets your AI agents debug production without redeploying") and `at8pm` ("your honest journal" — the diary interest area). A `gh api search/repositories` pass on each returned `total_count=0`; all four are closed-source SaaS and not buildable from a spec.

- **Also dropped before the table, on the 08-30 freshness rule** (an HN post's date is not the repo's date, so `created_at` gets checked before anything else): `oborchers/proqi` (22★, terminal-native prompt composer, Show HN today — created 08-23), `gageml/gage` (5★, scan Claude sessions for bugs, Show HN today — created 05-27), and `lovaxi/Rubato_Device` (retro-Mac desk companion mirroring AI coding state — created 08-30). All three read as today's finds from their HN placement alone. `stiifff/jarvis-workspace` (4★, local web cockpit running several coding agents side by side) was in-window but dropped as another instance of the Crystal/Conductor/vibe-kanban workbench lane, and `unpeel-com/unpeel` (32★, Swift + Rust multiplexer for always-on terminal agents) as another instance of the tmux-for-agents lane.

- **Source recipe unchanged and clean.** 10 sequential `gh api search/repositories` sweeps (`created:>=2026-09-04`, `sleep 2` between, `.description[0:130]` truncated inside the jq filter per the 08-31 lesson) → 11 shortlisted repos after dedup against `state/seen.json`, all 11 confirmed by a direct `repos/<owner>/<name>` fetch rather than trusted from the search page. 14 parallel HN Algolia single-word `search_by_date` calls with `created_at_i%3E1788480000` → 369 stories in the 48h window, of which ~60 Show HN / GitHub-linked items were relevant. 19 further `gh api` calls for prior-art verification. Reddit remains unfetchable and was not attempted. Product Hunt fetched from the homepage — `/categories/artificial-intelligence` now 404s.
